import { Injectable } from "@angular/core";
import { Http } from "@angular/http";
import { Meeting } from "../models";
import { Observable } from "rxjs";

import { apiCofiguration } from "../configuration";


@Injectable()
export class MeetingService {
    constructor(private _http: Http) { }

    public add(entity: Meeting) {
        return this._http
            .post(`${apiCofiguration.baseUrl}/api/meeting/add`, entity)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public get() {
        return this._http
            .get(`${apiCofiguration.baseUrl}/api/meeting/get`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public getById(options: { id: number }) {
        return this._http
            .get(`${apiCofiguration.baseUrl}/api/meeting/getById?id=${options.id}`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public remove(options: { id: number }) {
        return this._http
            .delete(`${apiCofiguration.baseUrl}/api/meeting/remove?id=${options.id}`)
            .map(data => data.json())
            .catch(err => {
                return Observable.of(false);
            });
    }

    public get baseUrl() { return apiCofiguration.baseUrl; }

}
